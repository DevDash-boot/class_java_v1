package com.oop10;

public class NumberPrintTest2 {

    public static void main(String[] args) {
        // NumberPrinter p1 = new NumberPrinter(1);
        // NumberPrinter p2 = new NumberPrinter(2);

        // static 변수는 모든 객체가 공유하는 변수를 만들 때 사용할 수 있다.
        // static 변수는 객체를 생성하기 전에 먼저 사용할 수 있다.
        // 그래서 클래스 변수라고 부른다.

        System.out.println(NumberPrinter.waitNum);

    }
}
